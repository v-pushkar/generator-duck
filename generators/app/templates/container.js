import React from 'react';
import { View } from 'react-native';

const <%= name.toUpperCase() %> = () => (
    <View>
            <h1>Tesxt: <%= name %></h1>
    </View>
);

export default <%= name %>;